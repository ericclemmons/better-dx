{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "catalog:",
    "@hono/node-server": "^1.14.4",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "ai": "catalog:",
    "@ai-sdk/google": "^2.0.51",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@better-dx/api": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@better-dx/config": "workspace:*"
  }
}
